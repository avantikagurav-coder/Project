pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/avantikagurav-coder/Project.git'
            }
        }

        stage('Build') {
            steps {
                echo "Build successful"
            }
        }
    }
}